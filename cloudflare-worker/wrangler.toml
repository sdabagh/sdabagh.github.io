name = "stats-tutor-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# This worker will be deployed to:
# https://stats-tutor-api.sdabagh-stats.workers.dev

# Environment variables (secrets)
# Add your Anthropic API key with: wrangler secret put ANTHROPIC_API_KEY
